mainmodule main
{
	readonly CProxy_Main mainProxy;
	readonly DoubleArray x;

	extern module row_slice;
	extern module row;
	extern module multi_row_slice;
 	
	mainchare Main
	{
		entry Main(CkArgMsg* msg);
		entry void setResult(int row, double res);
		entry void setResultMultiRow(int nRows, int firstRow, double res[nRows]);
		entry void stageFinished();
	};
};
